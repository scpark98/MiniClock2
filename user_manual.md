# MiniClock2 사용 설명서

컴퓨터가 익숙하지 않아도 따라 할 수 있도록 썼습니다.
**3번(시계 다루기)** 과 **4번(알람 만들기)** 만 읽어도 이 프로그램의 대부분을 쓰실 수 있습니다.

---

## 1. 어떤 프로그램인가요

바탕화면에 **항상 떠 있는 큰 시계**입니다. 여기에 **타이머(알람)** 기능이 붙어 있습니다.

이런 때 씁니다.

- "20분 뒤에 알려줘" — 라면 끓일 때, 세탁기 돌릴 때, 잠깐 쉴 때
- "오후 3시에 알려줘" — 회의, 약속
- 여러 개를 한꺼번에 걸어두고 목록으로 관리
- 지금 가장 임박한 알람의 남은 시간을 바탕화면에 크게 띄워두기

컴퓨터를 켜면 자동으로 실행됩니다.

---

## 2. 처음 실행하면 무엇이 보이나요

| 보이는 것 | 설명 |
|---|---|
| **큰 숫자 시계** | 바탕화면에 떠 있는 현재 시각. 이것이 이 프로그램의 본체입니다. |
| **작은 글씨의 GPU 정보** | 그래픽카드 사용률과 온도. 필요 없으면 끌 수 있습니다(7번). |
| **트레이 아이콘** | 화면 오른쪽 아래 시계 옆에 작은 아이콘이 생깁니다. |

처음에는 알람이 하나도 없으므로 알람 목록 창은 뜨지 않습니다.

---

## 3. 시계 다루기 — 마우스 조작

> ### 꼭 먼저 알아둘 것
> 시계를 **그냥 끌면 움직이지 않습니다.**
> 반드시 **Shift 키를 누른 채로** 끌어야 합니다.
> 작업 중에 실수로 시계를 옮기는 일을 막으려고 일부러 이렇게 만들어져 있습니다.

| 하고 싶은 것 | 조작 |
|---|---|
| 시계 **위치 옮기기** | **Shift** 를 누른 채 시계를 마우스로 끌기 |
| 시계 **글자 크기** 바꾸기 | **Shift** 를 누른 채 마우스 **휠**을 위/아래로 굴리기 |
| **알람 추가** 창 열기 | 시계를 **더블클릭** |
| **메뉴** 열기 | 시계 위에서 **마우스 오른쪽 버튼** 클릭 |

시계의 **글꼴과 색**은 오른쪽 클릭 메뉴에서 바꿉니다(7번). 크기만 휠로 바꾼다고 기억하세요.

---

## 4. 알람 만들기 — 가장 많이 쓰는 기능

### 따라 하기

1. 시계를 **더블클릭**합니다. (또는 오른쪽 클릭 → `알람 추가...`)
2. `알람 추가` 창이 뜹니다.
3. **시간** 칸에 `20` 이라고 칩니다. → 20분 뒤에 알려달라는 뜻입니다.
4. **제목** 칸에 `라면` 이라고 칩니다. (비워도 됩니다. 그러면 `제목없음0` 처럼 자동으로 붙습니다.)
5. **확인** 을 누릅니다.

끝입니다. 20분 뒤에 "삐" 소리와 함께 `라면` 이라는 창이 뜹니다.

### 시간 칸에 넣을 수 있는 형식

이 칸이 이 프로그램에서 제일 중요합니다.

| 이렇게 쓰면 | 이런 뜻입니다 |
|---|---|
| `20` | **20분 뒤** |
| `90` | **90분 뒤** (1시간 30분 뒤) |
| `9:30` | **오늘 9시 30분** 에 |
| `0930` | **9시 30분** 에 |
| `1.30` | **1시간 30분 뒤** (점으로 구분) |
| `1 30` | **1시간 30분 뒤** (빈칸으로 구분) |
| `1d 2h 3m` | **1일 2시간 3분 뒤** (d=일, h=시간, m=분) |

**헷갈리기 쉬운 두 가지**

- `130` → 숫자 **3자리**는 "130분 뒤"
- `0130` → 숫자 **4자리**는 "1시 30분에"

즉 숫자만 넣을 때 **4자리면 시각, 그 외에는 분** 으로 봅니다.

`9:30` 처럼 시각으로 지정했는데 그 시각이 이미 지났다면 남은 시간이 음수가 되어 목록에서 바로 지난 항목으로 처리됩니다. 다음 날 알람을 원하시면 `1d` 를 함께 쓰거나 분 단위로 계산해 넣으세요.

### 즐겨찾기

`알람 추가` 창의 **즐겨찾기** 를 체크하고 확인하면 그 알람이 따로 저장됩니다.
다음부터는 **알람 목록 창에서 오른쪽 클릭**하면 메뉴 맨 아래에 `라면 (20분) (&1)` 처럼 나타나고, 그것을 누르면 같은 알람이 바로 추가됩니다. 자주 쓰는 타이머를 등록해 두면 편합니다.

> **주의**: 현재 버전에는 **즐겨찾기를 지우는 기능이 없습니다.** 한 번 등록하면 메뉴에 계속 남습니다. 꼭 필요한 것만 등록하세요.

---

## 5. 알람 목록 창

### 여는 방법

- 시계 위에서 오른쪽 클릭 → `설정 시각 목록 보기`
- 또는 **F5** 키

F5 를 다시 누르면 닫힙니다.

### 창에 보이는 것

| 칸 이름 | 내용 |
|---|---|
| **이름** | 알람 제목 |
| **등록 시각** | 이 알람을 만든 시각 |
| **간격** | 얼마 뒤에 울리는지 |
| **알람 시각** | 실제로 울릴 시각 |
| **남은 시각** | 지금부터 얼마나 남았는지 (1초마다 갱신) |
| **등록 날짜** | 만든 날짜 |

목록은 **남은 시간이 짧은 순서로 자동 정렬**됩니다. 맨 위가 가장 임박한 알람입니다.
칸 제목을 눌러 정렬 순서를 바꿀 수는 없습니다. 항상 임박한 순입니다.

칸의 **너비**는 칸 제목 사이의 경계선을 끌어서 조절할 수 있고, 조절한 너비는 다음에 열 때도 유지됩니다.

### 창 다루기

- **옮기기**: 목록이 없는 빈 부분을 마우스로 끕니다. (시계와 달리 Shift 를 누르지 않아도 됩니다)
- **크기 바꾸기**: 창의 네 가장자리나 모서리를 끕니다.
- **Auto Hide** (창 오른쪽 위 체크): 켜두면 다른 프로그램을 클릭했을 때 이 창이 **저절로 숨습니다.** 다시 이 프로그램을 클릭하면 나타납니다. 화면을 깔끔하게 쓰고 싶을 때 켜세요.
- **빈 곳 더블클릭**: 알람 추가 창이 열립니다.

---

## 6. 목록 내용 고치기 (중요)

만들어 둔 알람의 **이름, 등록 시각, 간격, 알람 시각**을 목록에서 직접 고칠 수 있습니다.

### 편집 시작하는 두 가지 방법

**방법 1 — 클릭한 뒤 다시 클릭 (탐색기에서 파일 이름 바꾸듯이)**

1. 고칠 칸을 한 번 클릭합니다. → 그 줄이 선택됩니다.
2. **잠시 기다렸다가** 같은 칸을 **다시 한 번 클릭**합니다.
3. 그 칸이 입력 상태로 바뀝니다.

> 두 번 클릭이 너무 빠르면 "더블클릭"이 되어 편집이 아니라 알람 추가 창이 뜰 수 있습니다.
> **천천히, 한 박자 쉬고** 두 번째 클릭을 하세요.

**방법 2 — F2 키 (더 간단합니다)**

1. 고칠 **칸**을 한 번 클릭합니다. (그 칸이 기억됩니다)
2. **F2** 를 누릅니다. → 바로 입력 상태가 됩니다.

F2 는 **마지막으로 클릭한 칸**을 엽니다. 그래서 이름을 고치려면 이름 칸을, 간격을 고치려면 간격 칸을 먼저 클릭해야 합니다.

### 입력을 마칠 때

| 키 | 하는 일 |
|---|---|
| **Enter** | 고친 값을 **저장**합니다 |
| **Esc** | 고치기를 **취소**합니다 (원래 값 그대로) |

### 고칠 수 있는 칸과 효과

| 칸 | 고치면 |
|---|---|
| **이름** | 제목이 바뀝니다. |
| **등록 시각** | 시작 시각이 바뀌고, 그에 맞춰 알람 시각이 다시 계산됩니다. |
| **간격** | 알람 시각이 자동으로 다시 계산됩니다. |
| **알람 시각** | 간격이 자동으로 다시 계산됩니다. |

**남은 시각**과 **등록 날짜** 칸은 입력창이 열리더라도 값이 반영되지 않습니다. 이 둘은 다른 값에서 자동으로 계산되는 칸입니다.

간격이나 시각을 입력할 때는 4번의 시간 형식을 그대로 쓰시면 됩니다.

### 항목 지우기

1. 지울 항목을 클릭해서 고릅니다.
2. **Del** 키를 누릅니다. (또는 오른쪽 클릭 → `삭제`)

**여러 개를 한꺼번에** 지울 수도 있습니다. **Ctrl** 을 누른 채 여러 줄을 클릭하거나, 첫 줄을 클릭하고 **Shift** 를 누른 채 마지막 줄을 클릭해 범위로 고른 뒤 Del 을 누르세요.

---

## 7. 목록에서 오른쪽 클릭 메뉴

항목을 고른 뒤 오른쪽 클릭하면 나옵니다.

| 메뉴 | 하는 일 |
|---|---|
| **현재 시각으로 다시 시작** | 간격은 그대로 두고 지금부터 다시 셉니다. 20분 타이머가 5분 남았을 때 누르면 다시 20분이 됩니다. |
| **이 항목을 플로팅으로 표시** | 이 알람을 바탕화면에 크게 띄웁니다(8번). |
| **현재 시간부터 10분 타이머** | 지금부터 10분으로 바꿉니다. 제목도 `10분 타이머` 로 바뀝니다. |
| **현재 시간부터 20분 타이머** | 위와 같고 20분입니다. |
| **클립보드로 복사** | 항목 내용을 복사합니다. 메모장 등에 붙여넣을 수 있습니다. |
| **잠금** | 실수로 건드리지 않도록 보호합니다(아래). |
| **삭제** | 이 항목을 지웁니다. |

메뉴 맨 아래에는 등록해 둔 **즐겨찾기 목록**이 이어서 나옵니다.

항목을 고르지 않은 상태에서 오른쪽 클릭하면 대부분의 메뉴가 흐리게 표시됩니다. 고를 항목이 없으니 할 수 있는 일도 없기 때문입니다.

### 잠금이란

중요한 알람을 **실수로 지우거나 바꾸는 것을 막는** 기능입니다.

잠근 항목은 이렇게 됩니다.

- 목록에서 **회색 글씨**로 보입니다
- 오른쪽 클릭 메뉴에서 **다시 시작 / 10분 / 20분 / 플로팅 / 삭제** 가 전부 **흐리게 바뀌어 눌리지 않습니다**
- **Del** 키로도 지워지지 않습니다
- 목록에서 칸을 고쳐도 **원래 값으로 되돌아갑니다**

풀려면 다시 오른쪽 클릭 → `잠금` 을 누르세요. (체크 표시가 사라집니다)

> **"삭제가 안 돼요" 의 대부분이 이 경우입니다.** 지우려는 항목이 잠겨 있는지 먼저 확인해 보세요.

---

## 8. 알람이 울리면 / 플로팅 표시

### 울릴 때

- **"삐"** 소리가 나고
- 알람 **제목이 적힌 창**이 뜹니다. **10초 뒤 저절로 닫힙니다.** 그냥 두셔도 됩니다.

### 울린 뒤

- 목록에서 그 항목이 **붉은색**으로 바뀝니다 (시간이 지났다는 표시)
- **10분이 지나면 목록에서 저절로 사라집니다**

### 플로팅 표시

가장 임박한 알람 하나가 바탕화면에 **크게 두 줄**로 떠 있습니다.

```
라면
00:19:32
```

윗줄이 이름, 아랫줄이 남은 시각입니다. 목록 창을 열지 않아도 남은 시간을 볼 수 있습니다.
**Shift 를 누른 채 끌어서** 원하는 자리로 옮길 수 있습니다.

어느 항목이 플로팅으로 뜰지는 **남은 시간이 가장 짧은 것**으로 자동으로 정해집니다. 오른쪽 클릭 메뉴에서 직접 지정할 수도 있지만, 목록이 바뀌면 다시 자동으로 돌아갑니다.

---

## 9. 시계 모양과 설정 (오른쪽 클릭 메뉴)

| 메뉴 | 하는 일 |
|---|---|
| **폰트 설정...** | 시계 글꼴을 고릅니다. |
| **색상 선택...** | 시계 글자색을 고릅니다. |
| **항상 맨 위에** (Ctrl+T) | 다른 창에 가려지지 않게 합니다. 체크를 끄면 일반 창처럼 가려집니다. |
| **nVidia Info** | GPU 사용률·온도 표시를 켜고 끕니다. |
| **설정 시각 목록창을 메인 모니터로 이동** | 알람 목록 창이 화면 밖으로 나가 보이지 않을 때 되찾는 기능입니다. |
| **탐색기 재시작** | 아래 주의 참고. |

글자 **크기**는 메뉴가 아니라 **Shift + 마우스 휠** 로 바꿉니다.

> **탐색기 재시작 주의**
> 이 메뉴는 Windows 의 탐색기(작업 표시줄·바탕화면 아이콘을 담당)를 강제로 끄고 다시 켭니다.
> 누르면 **작업 표시줄이 잠깐 사라졌다가 돌아오고, 열려 있던 탐색기 창이 모두 닫힙니다.**
> 작업 표시줄이 이상해졌을 때 쓰는 복구용 기능이며, 평소에는 누를 일이 없습니다.

> **`기타 기능` → `투명도 설정...` 은 현재 동작하지 않습니다.** 눌러도 아무 일이 일어나지 않습니다.

---

## 10. 자주 쓰는 위치 저장하기

모니터를 여러 대 쓰거나 작업에 따라 시계 위치를 바꿔 쓸 때 편한 기능입니다.
오른쪽 클릭 → `기타 기능` 안에 있습니다.

1. 시계를 원하는 자리로 옮깁니다 (**Shift** + 드래그).
2. **F1** 을 누릅니다. → 그 위치가 1번으로 저장됩니다.
3. 나중에 어디에 있든 **1** 을 누르면 → 저장해 둔 자리로 돌아갑니다.

같은 방식으로 **F2 / F3** 에 저장하고 **2 / 3** 으로 불러옵니다.

> 알람 목록 창에서 항목을 고른 상태로 **F2** 를 누르면 위치 저장이 아니라 **칸 편집**(6번)이 시작됩니다.
> 위치 저장으로 쓰시려면 **시계를 클릭한 뒤에** 누르세요.

---

## 11. 시스템 종료 예약

오른쪽 클릭 → `시스템 종료 시각 설정...`

세 가지 방법 중 하나를 고르고 **확인** 을 누릅니다.

- **5분 후 ~ 60분 후** 중에서 고르기 (5분 단위 버튼)
- **`( ) 분 후 종료`** 칸에 원하는 분을 직접 입력
- **`시각 지정`** 을 고르고 `2330` 처럼 **hhmm 4자리**로 시각 입력

예약을 취소하려면 같은 창을 다시 열고 **종료 취소** 를 누르세요.

> 켜 두고 잠들 때 유용하지만, **저장하지 않은 작업이 있으면 잃을 수 있습니다.** 확인 후 사용하세요.

---

## 12. 트레이 아이콘 (화면 오른쪽 아래)

| 조작 | 하는 일 |
|---|---|
| **왼쪽 클릭** | 시계를 **숨기거나 다시 보이게** 합니다. 잠깐 화면을 깨끗이 쓰고 싶을 때. |
| **오른쪽 클릭** | 시계에서 오른쪽 클릭한 것과 같은 메뉴가 나옵니다. |

시계가 보이지 않을 때는 여기를 먼저 확인하세요.

---

## 13. 키보드·마우스 한눈에 보기

### 시계에서

| 키 / 조작 | 하는 일 |
|---|---|
| 더블클릭 | 알람 추가 |
| 오른쪽 클릭 | 메뉴 |
| **Shift** + 드래그 | 시계 옮기기 |
| **Shift** + 휠 | 글자 크기 |
| **F5** | 알람 목록 창 열기 / 닫기 |
| **Ctrl + T** | 항상 맨 위에 켜기 / 끄기 |
| **F1 / F2 / F3** | 지금 위치를 1 / 2 / 3번으로 저장 |
| **1 / 2 / 3** | 저장해 둔 1 / 2 / 3번 위치로 이동 |
| **Alt + F4** | 프로그램 끝내기 |

### 알람 목록 창에서

| 키 / 조작 | 하는 일 |
|---|---|
| 항목 클릭 후 **다시 클릭** | 그 칸 편집 시작 |
| **F2** | 마지막에 클릭한 칸 편집 시작 |
| **Enter** | 편집한 값 저장 |
| **Esc** | 편집 취소 |
| **Del** | 고른 항목 삭제 |
| **Ctrl / Shift** + 클릭 | 여러 항목 고르기 |
| 오른쪽 클릭 | 항목 메뉴 |
| 빈 곳 더블클릭 | 알람 추가 |
| 빈 곳 드래그 | 창 옮기기 |

> 메뉴를 열면 항목 오른쪽에 단축키가 함께 적혀 있습니다. 외우지 않아도 메뉴만 보면 됩니다.

---

## 14. 자동 시작

이 프로그램은 **컴퓨터를 켜면 자동으로 실행**되도록 스스로 등록합니다. 따로 설정할 것이 없습니다.

프로그램 **폴더를 옮겨도** 다음에 한 번 실행하면 자동 시작 위치가 새 폴더로 알아서 갱신되고, 옛 위치의 등록은 지워집니다. 마지막으로 실행한 위치가 기준이 됩니다.

---

## 15. 이럴 땐 이렇게

**시계가 안 움직여요**
→ **Shift** 를 누른 채로 끌어야 합니다(3번).

**목록에서 칸을 고치려는데 편집이 안 돼요**
→ 두 가지를 확인하세요.
1. 두 번 클릭이 **너무 빨랐을 수 있습니다.** 한 박자 쉬고 두 번째를 클릭하거나, 칸을 클릭한 뒤 **F2** 를 누르세요(6번).
2. 그 항목이 **잠겨** 있으면 고쳐도 원래 값으로 되돌아갑니다(7번).

**항목이 삭제가 안 돼요**
→ 그 항목이 **잠겨** 있습니다. 오른쪽 클릭 메뉴에서 `잠금` 앞에 체크가 있는지 보세요. 눌러서 해제한 뒤 지우면 됩니다(7번).

**알람 목록 창이 사라졌어요**
→ 세 가지를 확인하세요.
1. **Auto Hide** 가 켜져 있으면 다른 프로그램을 쓸 때 저절로 숨습니다. 시계를 클릭해 보세요.
2. **F5** 를 눌러 보세요.
3. 화면 밖으로 나갔을 수 있습니다. 오른쪽 클릭 → `설정 시각 목록창을 메인 모니터로 이동`.

**시계가 아예 안 보여요**
→ 트레이 아이콘(화면 오른쪽 아래)을 **왼쪽 클릭**하세요(12번).

**알람이 울렸는데 창을 못 봤어요**
→ 알람 창은 **10초 뒤 저절로 닫힙니다.** 목록에서 그 항목이 붉은색으로 남아 있고, 10분이 지나면 목록에서도 사라집니다.

**시계가 다른 창에 자꾸 가려져요**
→ 오른쪽 클릭 → `항상 맨 위에` 를 체크하세요(Ctrl+T).

**영상을 보는데 시계가 거슬려요**
→ 동영상 플레이어가 재생 중이면 시계가 **저절로 흐려집니다.** 마우스를 시계 위에 올리면 다시 진해집니다.

**즐겨찾기를 잘못 등록했어요**
→ 현재 버전에는 지우는 기능이 없습니다(4번).
